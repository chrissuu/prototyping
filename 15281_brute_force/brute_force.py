B = {
    ('+b') : 0.25,
    ('-b') : 0.75
}

E = {
    ('+e') : 0.15,
    ('-e') : 0.85
}

AgBE = {
    ('+a', '+b', '+e') : 0.8,
    ('-a', '+b', '+e') : 0.2, 
    ('+a', '+b', '-e') : 0.6,
    ('-a', '+b', '-e') : 0.4,
    ('+a', '-b', '+e') : 0.6,
    ('-a', '-b', '+e') : 0.4,
    ('+a', '-b', '-e') : 0.1,
    ('-a', '-b', '-e') : 0.9,
}

JgA = {
    ('+j', '+a') : 0.8,
    ('-j', '+a') : 0.2,
    ('+j', '-a') : 0.1,
    ('-j', '-a') : 0.9
}

MgA = {
    ('+m', '+a') : 0.65,
    ('-m', '+a') : 0.35,
    ('+m', '-a') : 0.4,
    ('-m', '-a') : 0.6
}